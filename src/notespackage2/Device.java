package notespackage2;
public abstract class Device { // abstract classes cannot be instantiated, but they can have a subclass
    
    abstract void connect(); // abstract methods are declared without an implementation (or without a body. think {})
}
